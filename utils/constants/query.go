package constants

const (
	WHERE_ID                    = "id = ?"
	WHERE_USERNAME_AND_PASSWORD = "user_name = ? AND password = ?"
	WHERE_USERNAME              = "user_name = ?"
	WHERE_USER_ID               = "user_id = ?"
	WHERE_ID_AND_USER_ID        = "id = ? AND user_id = ?"
)
